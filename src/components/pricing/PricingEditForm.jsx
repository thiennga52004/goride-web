const PricingEditForm = ({ config, onSubmit, onCancel, loading }) => {
  return (
    <form>
      <p style={{ color: 'var(--text-secondary)' }}>Pricing Edit Form — chờ implement</p>
    </form>
  );
};

export default PricingEditForm;
