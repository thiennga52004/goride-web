const TripFilters = ({ onSearch, onStatusFilter, onDateRangeChange }) => {
  return (
    <div className="trip-filters" style={{ display: 'flex', gap: '12px', marginBottom: '16px', flexWrap: 'wrap' }}>
      {/* TODO */}
      <p style={{ color: 'var(--text-secondary)' }}>Trip Filters — chờ implement</p>
    </div>
  );
};

export default TripFilters;
