const TripStatusHistory = ({ history = [] }) => {
  return (
    <div className="trip-status-history">
      {/* TODO: Audit log table */}
      <p style={{ color: 'var(--text-secondary)' }}>Status History — chờ implement</p>
    </div>
  );
};

export default TripStatusHistory;
