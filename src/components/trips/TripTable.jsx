const TripTable = ({ trips, loading }) => {
  return (
    <div className="trip-table">
      {/* TODO */}
      <p style={{ color: 'var(--text-secondary)', padding: '24px' }}>Trip Table — chờ implement</p>
    </div>
  );
};

export default TripTable;
