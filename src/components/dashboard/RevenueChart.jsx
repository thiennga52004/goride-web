const RevenueChart = ({ data }) => {
  return (
    <div className="revenue-chart" style={{ background: 'var(--glass-bg)', borderRadius: 'var(--radius-md)', padding: '24px', border: '1px solid var(--glass-border)' }}>
      <h3 style={{ color: 'var(--text-primary)', fontSize: '16px', marginBottom: '16px' }}>Doanh thu theo ngày</h3>
      <p style={{ color: 'var(--text-secondary)' }}>Recharts Line Chart — chờ implement</p>
    </div>
  );
};

export default RevenueChart;
