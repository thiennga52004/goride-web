const StatsOverview = ({ stats }) => {
  return (
    <div className="stats-overview">
      {/* TODO: Grid of 6 StatCards */}
      <p style={{ color: 'var(--text-secondary)', padding: '24px' }}>Stats Overview — chờ implement</p>
    </div>
  );
};

export default StatsOverview;
