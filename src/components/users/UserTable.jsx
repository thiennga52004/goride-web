const UserTable = ({ users, loading, onSort, sortField, sortDirection }) => {
  return (
    <div className="user-table">
      {/* TODO: DataTable for users */}
      <p style={{ color: 'var(--text-secondary)', padding: '24px' }}>User Table — chờ implement</p>
    </div>
  );
};

export default UserTable;
