const UserFilters = ({ onSearch, onRoleFilter, onStatusFilter }) => {
  return (
    <div className="user-filters" style={{ display: 'flex', gap: '12px', marginBottom: '16px', flexWrap: 'wrap' }}>
      {/* TODO: SearchInput, Role Select, Status Select */}
      <p style={{ color: 'var(--text-secondary)' }}>User Filters — chờ implement</p>
    </div>
  );
};

export default UserFilters;
